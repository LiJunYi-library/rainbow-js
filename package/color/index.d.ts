export declare function transitionColor(
  defColor: string,
  color: string
): (ratio: number) => string;

export declare function transitionStyleBgColor(
  defColor: string,
  color: string
): (ratio: number) => string;

export declare function transitionStyleColor(
  defColor: string,
  color: string
): (ratio: number) => string;
